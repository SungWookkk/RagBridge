import { UsageStatus } from "@/components/billing/usage-status";

/**
 * 사용량 현황 페이지
 *
 * @description
 * - 월간 사용량 및 한도 확인
 * - 실시간 사용량 모니터링
 * - 사용량 알림 및 임계값 설정
 */
export default function UsageStatusPage() {
  return <UsageStatus />;
}
