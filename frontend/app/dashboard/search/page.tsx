import { AISearch } from "@/components/search/ai-search";

/**
 * AI 검색 페이지
 *
 * @description
 * - 자연어로 질문하고 답변 받기
 * - RAG 기반 지능형 검색
 * - 출처 문서 및 하이라이트 표시
 * - 실시간 스트림 응답
 */
export default function AISearchPage() {
  return <AISearch />;
}