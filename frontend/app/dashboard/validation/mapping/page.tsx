import { AutoMappingBuilder } from "@/components/validation/auto-mapping-builder";

/**
 * 자동 매핑 페이지
 *
 * @description
 * - 필드 자동 매핑 규칙 설정 및 관리
 * - 문서 필드와 시스템 필드 간 매핑 규칙
 * - 매핑 정확도 및 성능 모니터링
 */
export default function AutoMappingPage() {
  return <AutoMappingBuilder />;
}
